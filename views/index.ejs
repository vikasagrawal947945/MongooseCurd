<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Whatsapp</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h2>-------------------- All Chats ------------------------</h2>
       <form method="get" action="http://localhost:3000/chats/new">
         <button> Create New Chat</button>
       </form>
    
     <%  for( let chat of chats) {%>
        <div class="chats">
         <p> From : <%= chat.from.toUpperCase() %></p>
         <p> Recived By : <%= chat.to %></p>
       <div >
        <p class="msg">Message : <%= chat.message%></p> 
         <form class="btn" method="GET" action="/chats/<%= chat._id %>/edit"> 
          <button>edit</button></form>
       <form class="btn" method="post" action="/chats/<%= chat._id %>?_method=DELETE" >
         <button>Delete Chat</button></form>

         <form class="btn" method="GET" action="/chats/<%= chat._id %>" >
          <button>Show Chat</button></form>
        </div>
        </div> 
        <hr>
        
        <% } %>
</body>
</html>